"""DB セッション・初期化（SQLite）。

本 issue ではプレースホルダ。後続 issue で SQLAlchemy の Engine /
SessionLocal / Base 定義・起動時 create_all を実装する（Alembic は使わない）。
"""

# TODO(後続 issue): create_engine(settings.database_url) / sessionmaker /
# DeclarativeBase / init_db()（create_all）を実装する。
