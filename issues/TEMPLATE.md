# issue: <タイトル — 例: 候補者一覧にスキル名フィルタを追加>

> このファイルをコピーして `issues/NNN-<短い英小文字スラッグ>.md`（例: `001-candidate-skill-filter.md`）として作成する。
> 番号は連番。記入できない欄は「なし」と明記する（空欄のまま放置しない）。

## ステータス

`open` / `in-progress` / `done`（着手時・完了時に更新する）

## 背景・目的

なぜこの変更が必要か。解決したい課題やユーザー価値を 1〜3 行で。

<!-- 例: ランキング画面で候補者が増えると目的のスキル保持者を探しにくい。スキル名で絞り込めるようにして選考を速くしたい。 -->

## 要件

実装してほしいことを箇条書きで。曖昧さを残さない（観測可能な振る舞いに落とす）。

- [ ] <!-- 例: GET /jobs/{id}/rankings に skill クエリを追加し、保有スキルに部分一致する候補者だけ返す -->
- [ ]

## 対象レイヤー

このうち触れるものに印を付ける（実装委譲先の判断に使う）。

- [ ] backend（`backend/` — FastAPI / SQLAlchemy / Pydantic / Ollama 連携・構造化・スコアリング）
- [ ] frontend（`frontend/` — Next.js App Router / React / TanStack Query / 候補者・求人・ランキング画面）

## 受け入れ条件（Definition of Done）

「これが満たされたら完了」と言える観測可能な条件を書く。サブエージェントはこれを満たすまで往復する。
触れたレイヤーの検証コマンドが通ることを前提に含める（backend: `ruff check` / `ruff format` / `mypy .` / `pytest`（Ollama 呼び出しはモック化）、frontend: `lint` / `build`）。

- [ ] <!-- 例: skill=react で絞ると React 保有者だけが返り、未指定なら全件返る（単体テストで担保） -->
- [ ]

## 確認したい受け入れシナリオ

ブラウザ実機で何が動けば OK か（既存導線を壊さない ＋ 今回の新規導線）。LLM 応答内容そのものに依存する判定は、人の受け入れ確認で担保する。
ブラウザ観測に変化がない変更（docs のみ・設定のみ・内部リファクタ）は、`なし（◯◯のため）` と書く。

- <!-- 例: ランキング画面でスキル名を入力すると一覧が絞り込まれ、クリアで全件に戻る -->

## スコープ外（やらないこと）

この issue では扱わないこと。CLAUDE.md「やらないこと」（認証 / マルチユーザ / Docker / Alembic / PostgreSQL / 外部 LLM API / 求人の保存後編集）に加え、個別に除外したいものがあれば。

-

## 参考

関連ファイル・CLAUDE.md / docs の該当箇所・既存実装へのリンクなど（`../backend/...` 等の相対パス）。

-
